{
  "name": "xscope",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first X/Twitter browser extension toolbox.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/content-script.js && node --check src/background.js"
  }
}
